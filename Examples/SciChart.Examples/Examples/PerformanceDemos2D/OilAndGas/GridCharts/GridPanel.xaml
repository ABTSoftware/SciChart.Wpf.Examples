<UserControl x:Class="SciChart.Examples.Examples.PerformanceDemos2D.OilAndGas.GridCharts.GridPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:l="clr-namespace:SciChart.Examples.Examples.PerformanceDemos2D.OilAndGas.GridCharts"
             xmlns:s="http://schemas.abtsoftware.co.uk/scichart">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="GridPanelResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <Style x:Key="TextBlockStyle" TargetType="TextBlock">
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                    <Setter Property="Foreground" Value="{StaticResource MainThemeSolidBrush}" />
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="Margin" Value="5,0" />
                </Style>
            </Grid.Resources>

            <TextBlock Grid.Column="0"
                       Style="{StaticResource TextBlockStyle}"
                       Text="GR" />
            <TextBlock Grid.Column="1"
                       Style="{StaticResource TextBlockStyle}"
                       Text="RHDB" />
            <TextBlock Grid.Column="2"
                       Style="{StaticResource TextBlockStyle}"
                       Text="NPHI" />
        </Grid>

        <l:GridLegend Grid.Row="1" />

        <ItemsControl Grid.Row="2"
                      Margin="-4"
                      ItemsSource="{Binding GridCharts}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding ColumnsCount, Mode=OneWay}"
                                 Rows="{Binding RowsCount, Mode=OneWay}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <s:SciChartSurface Margin="4"
                                       RenderableSeries="{s:SeriesBinding RenderableSeries}"
                                       XAxes="{s:AxesBinding XAxes}"
                                       YAxes="{s:AxesBinding YAxes}">
                        <s:SciChartSurface.RenderSurface>
                            <s:HighQualityRenderSurface/>
                        </s:SciChartSurface.RenderSurface>
                    </s:SciChartSurface>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <l:GridLegend Grid.Row="3" />
    </Grid>
</UserControl>